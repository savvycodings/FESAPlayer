import { SPACING } from '../../constants/layout'

/** Horizontal padding — matches Login (`containerPadding * 2`). */
export const ONBOARDING_HORIZONTAL = SPACING.containerPadding * 2

export const ONBOARDING_SECTION_GAP = SPACING.sectionGap

/** Approximate fixed footer (dots + pill button + padding, excluding safe area). */
export const ONBOARDING_FOOTER_BASE_HEIGHT = 120
